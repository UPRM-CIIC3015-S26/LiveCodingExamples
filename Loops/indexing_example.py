# create a function length(new_string)
# returns the length of a string
# def length(new_string):
#     return len(new_string)//2
#
# print(length("ABCD"))

# create a function middle(new_string)
# that returns the middle letter in new_string
# def middle(new_string):
#     return new_string[len(new_string)//2]
# # TODO what if par len
# print(middle("ABCDE"))

# create a function isVocal(letter)
# returns true if the letter is in the new_string, otherwise false
# def isVocal(letter):
#     vocals = "aeiou"
#     if letter in vocals:
#         return True
#     return False
#     return letter in "aeiou"

# print(isVocal("a"))

# word = "Hello World!"
# Use slicing to get 'Hello'
# Use slicing to get a substr that contains all letter in
# even indexes
# print(word[1::2])